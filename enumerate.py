name = "Name"
surname = "Surname"
for i in range(len(name)):
    item = name[i]
    # ... compute some result based on item ...
    name[i] = result


for i, item in enumerate(L):
    # ... compute some result based on item ...
    L[i] = result